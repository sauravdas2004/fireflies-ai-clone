export default function SettingsPage() {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-3xl font-semibold tracking-tight">Settings</h1>
        <p className="mt-2 text-sm text-muted">Workspace preferences, integrations, and account controls will live here.</p>
      </div>

      <div className="rounded-2xl border border-border bg-panel p-6 shadow-soft">
        <h2 className="text-lg font-medium">Coming Soon</h2>
        <p className="mt-2 text-sm text-muted">
          This assignment keeps authentication and integrations out of scope, so this page stays as a polished placeholder.
        </p>
      </div>
    </div>
  );
}
